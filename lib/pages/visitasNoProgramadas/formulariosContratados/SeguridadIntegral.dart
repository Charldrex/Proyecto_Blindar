import 'package:flutter/material.dart';

class FormularioSeguridadIntegral extends StatelessWidget {
  const FormularioSeguridadIntegral({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("FORMULARIO DE DIAGNOSTICO DE SEURIDAD INTEGRAL"),
      ),
    );
  }
}
