import 'package:flutter/material.dart';

class FormularioServicioTecnico extends StatelessWidget {
  const FormularioServicioTecnico({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("FORMULARIO DE SERVICIO TÉCNICO"),
        ),
        body: Card());
  }
}
