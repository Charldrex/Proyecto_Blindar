import 'package:flutter/material.dart';

class FormularioMantenimientoPreventivo extends StatelessWidget {
  const FormularioMantenimientoPreventivo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("FORMULARIO DE MANTENIMIENTO PREVENTIVO"),
      ),
    );
  }
}
